/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'via.placeholder.com',
      'pkavfoldfwsxerlvjirb.supabase.co'
    ],
  },
};

export default nextConfig;
